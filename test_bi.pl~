#!/usr/bin/env perl

use strict;
use warnings;

use MongoDB;

my $coll = MongoDB->new->get_database( 'test' )->get_collection( 'batch' );

my @docs = map { { some_number => rand 100_000 } } 1 .. 1_000_000;

$coll->batch_insert( \@docs );

